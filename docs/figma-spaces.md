# figma-spaces — 두 Figma 공간의 구조

조사일 **2026-08-08**. 모든 노드 ID는 REST API로 직접 확인한 값입니다.

## 공간 A — `Full_design` (읽기 전용 레퍼런스)

| 항목 | 값 |
|---|---|
| file key | `jjgud5OkeXKA4weyP3FEND` |
| URL | https://www.figma.com/design/jjgud5OkeXKA4weyP3FEND/Full_design |
| 생성자 | 홍선우 (QA 담당) |
| 폴더 | `개인` (personal drafts) |
| 토큰 권한 | `editor` — **권한은 있지만 쓰지 않습니다** |
| published components | **0개** — 컴포넌트 라이브러리가 아니라 생짜 프레임 모음 |

### 페이지 구성

| 페이지 | 노드 ID | 내용 |
|---|---|---|
| **Good** | `0:1` | 잘 만든 상세페이지 9종 + 컨셉/평가 섹션 8개 |
| **Bad** | `1:84541` | 못 만든 상세페이지 5종 + 코멘트 섹션 5개 |

### 공통 노드 구조

```
SECTION  (제품명, 폭 1500px)          ← 페이지의 최상위 단위
└── FRAME  (폭 860px, 길이 수만 px)   ← 실제 상세페이지 본체
    ├── FRAME '01'   (860 x 2672)     ← 블록(=장면) 단위
    ├── FRAME '02'   (860 x 1198)
    ├── FRAME '03'   (860 x 1751)
    └── ...
```

- **폭 860px**가 상세페이지의 사실상 표준입니다 (일부 861~862px).
- 블록 프레임은 **`01`, `02`, `03`… 두 자리 숫자**로 이름이 붙습니다. 예외적으로 `포인트2` 같은 의미 이름도 섞여 있습니다.
- ⚠️ **중간 FRAME 그룹핑은 제품마다 일관되지 않습니다.** 글로우샷은 860px 프레임 1개 아래 블록 14개지만, 구스밀은 860px 프레임이 22개로 쪼개져 있습니다. 따라서 신뢰할 수 있는 단위는 "SECTION" 과 "폭 860 프레임의 자식 블록"이며, 중간 계층 깊이를 가정하고 파싱하면 깨집니다.

### Good 페이지 인벤토리 (`0:1`)

| 제품 | SECTION ID | 크기 | 860 프레임 수 | 블록 수 | 컨셉 섹션 ID |
|---|---|---|---|---|---|
| 글로우샷 | `1:2` | 1500×38501 | 1 | 14 | `1:82808` |
| 꿀잠모드 | `1:539` | 1500×38501 | 9 | 23 | `1:82809` |
| 구스밀 | `1:1305` | 1500×48936 | 22 | 70 | `1:82810` |
| 올리빗 | `1:21812` | 1285×38621 | 1 | 21 | `1:82811` |
| 클렌즈 주스 | `1:57168` | 4943×38621 | 2 *(안 2개 병렬)* | 12 / 19 | `1:82812` |
| 일진와이어 | `1:82346` | 1500×38621 | 2 | 6 / 3 | `1:82813` |
| 밀밭명가 | `1:82814` | 1500×38501 | 1 | 17 | `1:84515` |
| 클렌즈 | `1:84516` | 1500×38796 | 1 | 14 | *(클렌즈_컨셉 공유)* |
| 파일변환 | `1:84542` | 1500×13055 | 1 | 6 | `1:84809` |

### Bad 페이지 인벤토리 (`1:84541`)

| 제품 | SECTION ID | 크기 | 860 프레임 수 | 블록 수 | 코멘트 섹션 ID |
|---|---|---|---|---|---|
| 상하키친 스프 | `1:84811` | 1500×27671 | 1 | 7 | `10:86241` |
| 간편블럭국 | `1:84822` | 1500×27671 | 1 | 8 | `10:86244` |
| 토마토 | `1:85419` | 1500×27671 | 9 | 74 | `10:86247` |
| **Section 4** ⚠️ | `1:85854` | 1500×27671 | 1 | 8 | `10:86250` (추정) |
| 감자 | `10:86226` | 1500×27671 | 1 | 7 | `10:86234` |

⚠️ **미확정 2건** — 사용자 확인 필요:
- `1:85854`는 이름이 `Section 4`로 남아 있어 제품명을 알 수 없음
- `코멘트` 섹션 중 **`토마토 코멘트`가 2개**(`10:86247`, `10:86250`). 내용상 `10:86250`("포근한(브라운 컬러)", 구매후기 언급)은 토마토가 아니라 `Section 4`의 코멘트로 보이나 확증 불가

### `_컨셉` / `코멘트` 섹션의 구조 (⭐ 이 프로젝트의 핵심 자산)

크기는 모두 1500×1471이고, **TEXT 노드 2개**만 들어 있습니다.

| 순서 | 내용 |
|---|---|
| 1번째 TEXT | **컨셉 키워드** 2~3줄 (예: `활기찬 / 건강한 / 다이어트`) |
| 2번째 TEXT | **`좋은 점` / `나쁜 점`** 평가 |

즉 이 파일은 단순한 스크린샷 모음이 아니라 **(컨셉 키워드 → 완성된 페이지 → 잘된 점/못된 점)이 짝지어진 라벨링된 코퍼스**입니다. Level 2의 AI 선행 QA를 학습·검증할 근거 데이터가 여기 있습니다.
추출된 규칙은 → [design-principles.md](./design-principles.md)

## 공간 B — Figma 프로젝트 `detailed_page_automation` (쓰기 대상)

**생성된 Figma 산출물이 쌓여야 하는 유일한 공간입니다.** 2026-08-08 쓰기 검증 완료.

| 항목 | 값 |
|---|---|
| 종류 | Figma **프로젝트** (파일이 아님) |
| **projectId** | **`636598794`** |
| 소속 팀 | `euijin lee's team` — planKey `team::1633815531118805138` |
| 계정 | `appevory@gmail.com` · **Full seat / pro** |
| 권한 | ✅ 쓰기 |

### 같은 팀의 다른 프로젝트 (⛔ 쓰기 금지)

혼동 방지를 위해 기록합니다. 아래는 **절대 쓰지 않습니다.**

| projectId | 이름 |
|---|---|
| 597088086 | `Team project` |
| 601372486 | `evory` |
| 597090360 | `lecture` |

개인 **Drafts 폴더**도 금지입니다 — `create_new_file`이 `projectId` 없이 호출되면 여기로 떨어지므로 주의. → [rules.md §1](./rules.md#1-쓰기-경계-가장-중요)

### 현재 내용

| file key | 이름 | 상태 |
|---|---|---|
| `PXrqYstoidmMfr2Ju6RMfP` | `_MCP_write_test` | 쓰기 검증용. 내용은 삭제 완료(빈 파일), **파일 껍데기는 UI에서 수동 삭제 필요** |

프로젝트는 원래 비어 있었고, 위 파일이 첫 번째였습니다.

### ⚠️ 파일 자체는 API로 삭제할 수 없습니다

Figma에는 **파일을 삭제하는 API가 없습니다.** OpenAPI 스펙의 DELETE 엔드포인트는 comment와 dev_resource 두 종류뿐이고, MCP 서버에도 파일 삭제 도구가 없습니다.

| 작업 | 가능 여부 |
|---|---|
| 파일 **내용**(노드) 삭제 | ✅ `use_figma` → `node.remove()` |
| 파일 **자체** 삭제 | ❌ API 불가 → Figma UI에서 우클릭 → Delete |

**함의:** 에이전트는 파일을 만들 수는 있어도 되돌릴 수 없습니다. 시행착오로 파일을 남발하면 사용자가 일일이 수동으로 지워야 하므로, **파일 생성은 신중하게** 하고 반복 작업은 기존 파일 안에서 페이지/프레임 단위로 처리하세요.

### projectId를 알아낸 경로

REST API에는 팀 목록 조회나 프로젝트 이름 검색 엔드포인트가 **없습니다.** 하지만 MCP `whoami`가 planKey(`team::{team_id}`)를 반환하므로, 거기서 team_id를 뽑으면 REST가 열립니다.

```
mcp__figma__whoami  →  team::1633815531118805138
     ↓
GET /v1/teams/1633815531118805138/projects   → detailed_page_automation = 636598794
GET /v1/projects/636598794/files             → 파일 목록
```

### 쓰기 방법

REST API로는 프레임/컴포넌트를 만들 수 없습니다(404). canvas 쓰기는 **Figma 공식 원격 MCP 서버**(`https://mcp.figma.com/mcp`)를 씁니다 — 2026년 2월 Claude Code 파트너십으로 출시된 write 기능. 상세는 → [rules.md §3](./rules.md#3-figma-api-능력-경계-아키텍처-결정-사항)

즉 이 프로젝트는 **읽기는 REST, 쓰기는 MCP**로 경로가 갈립니다.

## 유용한 REST 호출 예시

```bash
# 토큰 로드 (source 금지 — 포맷 때문에 깨짐)
TOKEN=$(python3 -c "
import re
for l in open('/Users/euijin/projects/detailed_page_automation/.env'):
    m = re.match(r\"\s*FIGMA_API_KEY\s*=\s*['\\\"]?([^'\\\"\s]+)\", l)
    if m: print(m.group(1)); break")

# 페이지 목록 (얕게)
curl -s -H "X-Figma-Token: $TOKEN" \
  "https://api.figma.com/v1/files/jjgud5OkeXKA4weyP3FEND?depth=1"

# 특정 섹션의 블록 구조
curl -s -H "X-Figma-Token: $TOKEN" \
  "https://api.figma.com/v1/files/jjgud5OkeXKA4weyP3FEND/nodes?ids=1:2&depth=2"

# 파일 메타 (폴더명, 소유자, 권한)
curl -s -H "X-Figma-Token: $TOKEN" \
  "https://api.figma.com/v1/files/jjgud5OkeXKA4weyP3FEND/meta"
```

> 전체 파일을 `depth` 없이 부르지 마세요. 3~7만 px 길이 페이지 14개라 응답이 매우 큽니다.
